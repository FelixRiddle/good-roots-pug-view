// Get login
let login = document.getElementById("navbar:session:login");

if(login) {
    // Hook up event
    login.addEventListener("click", (e) => {
        // e.preventDefault();
        // window.location.href = `${websiteInfo.baseUrl}/user/auth/login`
    });
}
