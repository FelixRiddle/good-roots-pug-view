import MarkPositionManager from "../../../lib/map/MarkPositionManager.js";

let markPositionManager = new MarkPositionManager();

let streetTag = document.getElementById("street");
if(streetTag) {
    
}

let countryTag = document.getElementById("country");
if(countryTag) {
    
}
