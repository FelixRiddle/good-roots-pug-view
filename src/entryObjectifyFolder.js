/** Entry objectify folder
 * 
 * Takes a given folder and converts it to a webpack entry containing all the files inside it.
 * 
 */
function entryObjectifyFolder() {
    
}

export default entryObjectifyFolder;
