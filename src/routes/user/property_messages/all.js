import express from "express";
import GeneralPropertyInformation from "../../../lib/model/GeneralPropertyInformation";

const allRouter = express.Router();

allRouter.get("/all/:id", async (req, res) => {
    
});

export default allRouter;

